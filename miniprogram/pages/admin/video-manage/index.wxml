<custom-nav-bar
  title="视频管理"
  showBack="{{true}}"
  rightText="上传"
  bindRightTap="goUpload"
></custom-nav-bar>

<view class="page">
  <view class="top-bar">
    <view class="tab-btn" bindtap="goChannelManage">
      <text>视频号管理</text>
      <text class="iconfont icon-right"></text>
    </view>
  </view>

  <view wx:if="{{videoList.length === 0 && !loading}}" class="empty">
    <text class="empty-text">暂无视频</text>
    <view class="empty-btn" bindtap="goUpload">去上传</view>
  </view>

  <view class="video-list" wx:if="{{videoList.length > 0}}">
    <view
      wx:for="{{videoList}}"
      wx:key="_id"
      class="video-item"
      data-id="{{item._id}}"
      bindtap="onVideoTap"
    >
      <view class="item-cover">
        <image wx:if="{{item.coverUrl}}" class="cover-img" src="{{item.coverUrl}}" mode="aspectFill"></image>
        <view wx:else class="cover-placeholder">
          <text>{{item.title}}</text>
        </view>
        <view class="duration" wx:if="{{item.duration}}">{{item.duration}}"</view>
      </view>
      <view class="item-info">
        <view class="item-title">{{item.title}}</view>
        <view class="item-meta">
          <text class="meta-author">{{item.figureName}}</text>
          <text class="meta-stats">♥ {{item.likeCount || 0}}</text>
        </view>
      </view>
      <view class="item-delete" catchtap="onDelete" data-id="{{item._id}}">
        <text class="iconfont icon-delete"></text>
      </view>
    </view>
  </view>
</view>
