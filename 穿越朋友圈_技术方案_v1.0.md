# 穿越朋友圈 — 小程序技术方案 v1.0

> 参考项目：`intelligentmoms`（智慧妈妈）微信小程序
> 制定日期：2026-07-24

---

## 一、技术选型

基于参考项目 `intelligentmoms` 的实践，以及穿越朋友圈的产品需求，技术选型如下：

| 层级 | 选型 | 理由 |
|------|------|------|
| **框架** | 微信小程序原生框架 | 与参考项目一致，零额外依赖，微信风格还原度最高 |
| **语言** | JavaScript (ES6+) | 参考项目采用纯 JS，增强编译已启用，支持现代语法 |
| **样式** | WXSS + CSS 变量 | 参考项目使用 CSS 变量构建设计系统，便于主题统一管理 |
| **UI 组件** | 全自定义组件 | 参考项目无第三方 UI 库，微信风格需要高度定制，自研组件最灵活 |
| **状态管理** | `globalData` + 自定义发布订阅 | 参考项目模式，满足小程序体量需求，无需引入 Redux/MobX |
| **后端** | 微信云开发（CloudBase） | 参考项目全云开发架构，云函数 + 云数据库 + 云存储一站式解决 |
| **AI 接口** | 云函数代理调用 DeepSeek API / GLM-4 | 小程序端不能直接调用外部 AI API，需云函数代理（参考项目 agent-ui 模式） |
| **24史数据** | 云函数代理调用穿越兰台主站 API | 复用主站 `timeslip-shiji` 的数据能力 |
| **用户体系** | 微信云开发匿名登录 + `openid` | 参考项目模式，自动登录/注册，零门槛使用 |
| **内容安全** | 微信 `msgSecCheck` / `imgSecCheck` | 参考项目 `contentCheck` 云函数模式，社区评论/用户输入必审 |
| **动效** | 微信小程序原生动画 + Lottie | 飞鸽传书动画使用 Lottie，其余用原生 CSS/JS 动画 |
| **构建** | 微信开发者工具原生编译 | 与参考项目一致 |

### 为什么不使用第三方 UI 库（如 Vant Weapp）？

参考项目 `intelligentmoms` 未引入任何第三方 UI 库，全部组件自研。穿越朋友圈对微信原生风格的还原度要求极高（聊天列表、聊天气泡、朋友圈信息流、TabBar 等），第三方 UI 库无法直接提供微信风格的组件，反而会引入不必要的样式覆盖成本。自研组件更轻量、更可控。

---

## 二、项目目录结构

```
timeslip-mini/
├── miniprogram/                          # 小程序源码（与参考项目一致）
│   ├── app.js                            # 应用入口
│   ├── app.json                          # 全局配置/页面路由/TabBar
│   ├── app.wxss                          # 全局样式 + CSS 变量
│   ├── sitemap.json                      # 搜索配置
│   │
│   ├── pages/                            # 页面
│   │   ├── chat/
│   │   │   ├── index.js / .wxml / .wxss / .json   # 聊天列表页（Tab 1）
│   │   │   └── room.js / .wxml / .wxss / .json    # 对话详情页
│   │   ├── lantai/
│   │   │   ├── index.js / .wxml / .wxss / .json   # 兰台首页（Tab 2）
│   │   │   ├── figure-detail.js / ...             # 人物图鉴详情
│   │   │   └── book-reader.js / ...               # 24史阅读页
│   │   ├── discover/
│   │   │   ├── index.js / .wxml / .wxss / .json   # 发现页（Tab 3）
│   │   │   ├── moment-detail.js / ...             # 朋友圈详情
│   │   │   ├── dna-test.js / ...                  # 历史人格测试
│   │   │   ├── pigeon.js / ...                    # 飞鸽传书
│   │   │   └── memorial.js / ...                  # 批奏折
│   │   └── profile/
│   │       ├── index.js / .wxml / .wxss / .json   # 个人中心（Tab 4）
│   │       ├── letters.js / ...                   # 信鸽驿站
│   │       ├── achievements.js / ...              # 穿越成就
│   │       └── settings.js / ...                  # 设置
│   │
│   ├── components/                       # 自定义组件（全自研，参考 intelligentmoms）
│   │   ├── custom-nav-bar/               # 自定义导航栏（全局）
│   │   ├── custom-tab-bar/               # 自定义 TabBar（全局，参考项目 custom-tab-bar）
│   │   ├── chat-bubble/                  # 聊天气泡（左古纸/右微信绿）
│   │   ├── chat-input-bar/               # 聊天输入栏
│   │   ├── moment-card/                  # 朋友圈动态卡片
│   │   ├── moment-comment/               # 朋友圈评论组件
│   │   ├── figure-avatar/                # 人物古风头像
│   │   ├── memorial-scroll/              # 奏折卷轴组件
│   │   ├── pigeon-lottie/                # 飞鸽动画容器
│   │   ├── wd-markdown/                  # Markdown 渲染（参考项目 agent-ui/wd-markdown）
│   │   └── loading-skeleton/             # 骨架屏
│   │
│   ├── utils/                            # 工具模块（参考项目结构）
│   │   ├── auth.js                       # 用户认证（自动登录/注册）
│   │   ├── cloudRequest.js               # 云函数请求封装（含 loading/错误处理）
│   │   ├── db.js                         # 云数据库实例导出
│   │   ├── storage.js                    # 本地缓存封装
│   │   ├── date.js                       # 日期/历史纪年转换
│   │   └── globalLogic.js                # 全局状态工具（参考项目）
│   │
│   └── assets/                           # 静态资源
│       ├── images/                       # 图片（头像/背景/图标）
│       ├── lottie/                       # Lottie 动画文件
│       └── fonts/                        # 字体（Noto Serif SC / Ma Shan Zheng）
│
├── cloudfunctions/                       # 云函数（参考项目 9 个云函数模式）
│   ├── getUser/                          # 查询/自动创建用户
│   ├── chat/                             # AI 对话（调用 DeepSeek/GLM-4）
│   ├── moment/                           # 朋友圈数据（列表/点赞/评论）
│   ├── pigeon/                           # 飞鸽传书（写信/收信）
│   ├── memorial/                         # 批奏折（奏折获取/决策/推演）
│   ├── dna/                              # 历史人格测试（题目/匹配）
│   ├── shiji/                            # 24史数据代理（调用主站 API）
│   ├── contentCheck/                     # 内容安全审核（参考项目模式）
│   └── initDB/                           # 数据库初始化/数据导入
│
├── database_rules.json                   # 云数据库安全规则
└── project.config.json                   # 项目配置
```

---

## 三、全局配置设计

### 3.1 app.json — 页面路由与 TabBar

```json
{
  "pages": [
    "pages/chat/index",
    "pages/chat/room",
    "pages/lantai/index",
    "pages/lantai/figure-detail",
    "pages/lantai/book-reader",
    "pages/discover/index",
    "pages/discover/moment-detail",
    "pages/discover/dna-test",
    "pages/discover/pigeon",
    "pages/discover/memorial",
    "pages/profile/index",
    "pages/profile/letters",
    "pages/profile/achievements",
    "pages/profile/settings"
  ],
  "tabBar": {
    "custom": true,
    "list": [
      { "pagePath": "pages/chat/index", "text": "穿越" },
      { "pagePath": "pages/lantai/index", "text": "兰台" },
      { "pagePath": "pages/discover/index", "text": "发现" },
      { "pagePath": "pages/profile/index", "text": "我的" }
    ]
  },
  "window": {
    "navigationStyle": "custom",
    "backgroundColor": "#EDEDED",
    "backgroundTextStyle": "dark"
  },
  "requiredBackgroundModes": ["audio"],
  "lazyCodeLoading": "requiredComponents"
}
```

### 3.2 app.wxss — CSS 变量设计系统

参考 `intelligentmoms` 的 CSS 变量方案，定义微信风格 + 古风点缀的变量：

```css
/* 微信原生配色 */
--wechat-green: #07C160;
--wechat-bg: #EDEDED;
--wechat-white: #FFFFFF;
--wechat-text: #191919;
--wechat-text2: #888888;
--wechat-divider: #D9D9D9;
--wechat-blue: #576B95;
--wechat-red: #FA5151;

/* 古风点缀 */
--ancient-gold: #C9A24D;
--ancient-paper: #FFF9F0;
--ancient-ink: #2D1810;
--ancient-seal: #B71C1C;

/* 布局 */
--radius-sm: 4rpx;
--radius-md: 8rpx;
--radius-lg: 16rpx;
--radius-full: 999rpx;
```

---

## 四、核心功能模块技术实现

### 4.1 TabBar — 自定义 TabBar（参考 intelligentmoms）

- **方案**：`custom-tab-bar` 组件，与参考项目完全一致
- **样式**：底部四个 Tab（💬 聊天 / 📖 兰台 / 🧭 发现 / 👤 我的）
- **特点**：选中态使用微信绿 `#07C160`，未选中态使用 `#888888`
- **实现**：通过 `getTabBar().setData({ selected: 2 })` 在页面 `onShow` 中切换状态

### 4.2 AI 聊天模块

#### 4.2.1 聊天列表页（pages/chat/index）

- **UI**：完全模拟微信聊天列表
- **数据结构**：
  ```json
  {
    "figureId": "simaqian",
    "figureName": "司马迁 · 太史公",
    "avatar": "...",
    "lastMessage": "太史公曰：网罗天下放失旧闻...",
    "lastTime": 1721800800,
    "unreadCount": 2
  }
  ```
- **存储**：云数据库 `chat_sessions` 集合，按 `lastTime` 倒序

#### 4.2.2 对话详情页（pages/chat/room）

- **UI**：左侧古纸气泡 / 右侧微信绿气泡，底部输入栏
- **消息存储**：云数据库 `messages` 集合，分页加载（每次 20 条）
- **AI 调用流程**：
  ```
  用户发送消息
    → 前端本地追加消息（ optimistic update ）
    → 调用 cloudfunction: chat
    → 云函数构造 Prompt（人物身份注入）
    → 云函数调用 DeepSeek API / GLM-4
    → 流式返回（云函数返回字符串，前端逐字显示）
    → 保存 AI 回复到 messages 集合
  ```
- **Prompt 模板**：
  ```
  你是{人物}（{朝代}{身份}），性格：{描述}。
  用半文半白风格回复，引用真实著作。
  控制在100字内。
  用户问：{用户输入}
  ```

#### 4.2.3 与参考项目的差异

参考项目 `intelligentmoms` 使用微信原生 AI 能力（`wx.getAIInference` 等，基础库 >= 3.7.7），通过 `agent-ui` 组件直接连接微信 AI。

穿越朋友圈**不使用微信 AI 能力**，原因：
- 需要调用 DeepSeek / GLM-4（外部 API），微信 AI 不支持
- 需要自定义 Prompt 工程（人物身份注入）
- 因此采用**云函数代理模式**：前端 → 云函数 → DeepSeek API

### 4.3 朋友圈模块（pages/discover/index + moment-detail）

#### 4.3.1 数据策略

与产品方案一致：**朋友圈内容赛前预生成**，AI 仅负责评论回复。

- **预生成内容存储**：云数据库 `moments` 集合
- **数据结构**：
  ```json
  {
    "_id": "...",
    "figureId": "liubang",
    "figureName": "刘邦 · 汉高祖",
    "avatar": "...",
    "content": "今日鸿门，气氛有点微妙...",
    "images": ["..."],
    "dynasty": "han",
    "historicalEvent": "鸿门宴",
    "historicalDate": "公元前206年",
    "likes": [{"openid": "...", "name": "范增"}],
    "comments": [
      {"openid": "system", "name": "范增", "content": "竖子不足与谋！"},
      {"openid": "system", "name": "项羽", "content": "大哥别走啊🤔"}
    ],
    "createdAt": 1721800800
  }
  ```

#### 4.3.2 前端实现

- **信息流**：`scroll-view` + 分页加载（`skip`/`limit`）
- **点赞**：点击后本地 optimistic update + 云函数更新
- **评论**：用户评论后，调用 `cloudfunction: chat`（Prompt 为朋友圈评论模式），AI 以其他人物口吻回复
- **筛选**：顶部横向滚动条，按朝代筛选（先秦/秦汉/三国/唐/宋/明清）

### 4.4 批奏折模块（pages/discover/memorial）

#### 4.4.1 核心玩法技术实现

- **奏折数据**：预存储在云数据库 `memorials` 集合
- **数据结构**：
  ```json
  {
    "_id": "...",
    "chapter": "大汉篇",
    "dynasty": "han",
    "title": "韩信请封齐王奏",
    "content": "齐地反复，非假王不可镇抚...",
    "submitter": "韩信",
    "options": [
      {"key": "A", "text": "准奏封王", "hint": "..."},
      {"key": "B", "text": "驳回", "hint": "..."},
      {"key": "C", "text": "削其兵权后再封", "hint": "..."}
    ],
    "historicalOutcome": "真实：刘邦怒而想打，张良踩脚暗示后准奏...",
    "order": 1,
    "prerequisites": []
  }
  ```
- **决策流程**：
  ```
  用户阅读奏折 → 选择决策 → 调用 cloudfunction: memorial
  → 云函数构造推演 Prompt → 调用 DeepSeek API
  → 返回：决策逻辑 + 3个月后发展 + 新的奏折内容
  → 前端渲染"朱批"效果 + 展示推演结果
  ```
- **任务线**：`prerequisites` 字段控制前置任务，`user_progress` 集合记录用户完成状态

#### 4.4.2 朱批交互

- 用户在奏折底部用手指/触摸"书写"批示
- 使用 `<canvas>` 组件实现手写轨迹记录
- 批示文字作为图片临时保存，同时提取文本送入 AI 推演

### 4.5 飞鸽传书模块（pages/discover/pigeon）

#### 4.5.1 写信 + 回信流程

- **写信页**：富文本输入（最多 300 字），选择收信人物
- **飞鸽动画**：使用 Lottie 组件（`pigeon-lottie`），播放飞鸽穿越云层动画
- **AI 回信**：调用 `cloudfunction: pigeon`，Prompt 为飞鸽传书模式
- **信件存储**：云数据库 `letters` 集合，用户可在"信鸽驿站"查看历史信件

#### 4.5.2 Lottie 动画实现

参考项目未使用 Lottie，但飞鸽传书是该产品的核心视觉亮点：
- 动画文件：`assets/lottie/pigeon-fly.json`
- 使用微信小程序 `lottie-miniprogram` 库（或官方 `wx.createLottieContext`）
- 播放时机：用户发送信件后，全屏播放 3-5 秒飞鸽动画，然后展示回信

### 4.6 兰台模块（pages/lantai/index）

#### 4.6.1 24史数据接入

- **方案**：云函数 `shiji` 作为代理，调用穿越兰台主站 API
- **主站 API 地址**：`https://shiji.timeslip.work/api/...`
- **数据流**：
  ```
  小程序 → cloudfunction: shiji → Cloudflare Worker (timeslip-shiji) → D1 数据库
  ```
- **阅读器**：`scroll-view` + 分页加载，支持原文/译文切换

#### 4.6.2 人物图鉴

- **数据来源**：与朋友圈人物数据共享，存储在 `figures` 集合
- **卡片展示**：瀑布流布局（`wx:for` + 双列）
- **人物主页**：头像 + 生平 + 关联史书 + 关联朋友圈动态 + 一键聊天/写信入口

### 4.7 历史人格 DNA 测试（pages/discover/dna-test）

- **题目存储**：云数据库 `dna_questions` 集合（6-8 题）
- **匹配逻辑**：前端根据选项计算权重，匹配最接近的历史人格
- **结果展示**：
  - 人物画像
  - 匹配度百分比
  - 雷达图（使用 `<canvas>` 绘制）
  - 可生成海报分享（`wx.canvasToTempFilePath`）
- **人格类型定义**：`dna_types` 集合，预定义 8-10 种类型

### 4.8 个人中心（pages/profile/index）

参考微信"我"页面风格：
- 顶部：用户头像 + 昵称（可自定义古风名号）+ "穿越编号"
- 列表项：历史人格测试结果、穿越护照（成就墙）、信鸽驿站、聊天记录管理、设置
- 成就系统：`achievements` 集合，完成奏折任务/聊天/朋友圈互动解锁

---

## 五、数据库设计（云数据库集合）

参考 `intelligentmoms` 的数据库规则模式，设计以下集合：

| 集合 | 说明 | 读权限 | 写权限 |
|------|------|--------|--------|
| `users` | 用户信息（openid、昵称、古风名号、穿越编号） | 仅自己 | 仅自己 |
| `chat_sessions` | 聊天会话列表 | 仅自己 | 仅自己 |
| `messages` | 聊天消息 | 仅自己 | 仅自己 |
| `moments` | 朋友圈动态（预生成） | 公开 | 禁止（云函数写入） |
| `moment_comments` | 朋友圈用户评论 | 公开 | 禁止（云函数写入） |
| `moment_likes` | 朋友圈点赞 | 公开 | 禁止（云函数写入） |
| `letters` | 飞鸽传书信件 | 仅自己 | 禁止（云函数写入） |
| `memorials` | 奏折内容（预生成） | 公开 | 禁止（云函数写入） |
| `user_memorial_progress` | 用户奏折进度 | 仅自己 | 禁止（云函数写入） |
| `dna_questions` | DNA 测试题目 | 公开 | 禁止 |
| `dna_types` | DNA 人格类型 | 公开 | 禁止 |
| `figures` | 历史人物资料 | 公开 | 禁止 |
| `achievements` | 成就定义 | 公开 | 禁止 |
| `user_achievements` | 用户成就解锁记录 | 仅自己 | 禁止（云函数写入） |
| `content_audit_log` | 内容审核日志 | 仅自己 | 禁止 |

---

## 六、云函数详细设计

### 6.1 云函数列表

| 云函数 | 功能 | 调用场景 |
|--------|------|----------|
| `getUser` | 查询/自动创建用户 | `app.onLaunch` |
| `chat` | AI 对话（通用 Prompt 接口） | 聊天页、朋友圈评论回复 |
| `moment` | 朋友圈列表/点赞/评论 | 发现页 |
| `pigeon` | 飞鸽传书（写信/收信） | 飞鸽传书页 |
| `memorial` | 奏折获取/决策/推演 | 批奏折页 |
| `dna` | DNA 测试题目/结果匹配 | DNA 测试页 |
| `shiji` | 24史数据代理 | 兰台页 |
| `contentCheck` | 内容安全审核 | 发布评论/发送消息/写信前 |
| `initDB` | 数据库初始化 | 开发部署 |

### 6.2 通用 AI 云函数（chat）设计

参考 `intelligentmoms` 的 `agent-ui` 模式，但改为代理外部 AI API：

```javascript
// cloudfunctions/chat/index.js
const cloud = require('wx-server-sdk');
cloud.init();

exports.main = async (event, context) => {
  const { mode, figureId, userInput, history = [] } = event;

  // 根据 mode 选择 Prompt 模板
  let prompt = '';
  switch (mode) {
    case 'chat':
      prompt = buildChatPrompt(figureId, userInput, history);
      break;
    case 'moment_comment':
      prompt = buildMomentCommentPrompt(figureId, userInput);
      break;
    case 'pigeon_reply':
      prompt = buildPigeonPrompt(figureId, userInput);
      break;
    case 'memorial_simulate':
      prompt = buildMemorialPrompt(event.memorialId, event.decision);
      break;
  }

  // 调用 DeepSeek API
  const response = await callDeepSeek(prompt);
  return { content: response };
};
```

### 6.3 内容安全审核（contentCheck）

完全参考 `intelligentmoms` 的实现：

```javascript
// cloudfunctions/contentCheck/index.js
const cloud = require('wx-server-sdk');
cloud.init();

exports.main = async (event) => {
  const { content, image } = event;
  try {
    if (content) {
      await cloud.openapi.security.msgSecCheck({
        content,
        scene: 2, // 评论场景
        version: 2
      });
    }
    if (image) {
      await cloud.openapi.security.imgSecCheck({ media: { ... } });
    }
    return { pass: true };
  } catch (e) {
    return { pass: false, reason: e.message };
  }
};
```

---

## 七、用户认证与登录

参考 `intelligentmoms` 的认证流程：

```
app.onLaunch()
  → initUser()
    → wx.cloud.callFunction({ name: 'getUser' })
      → 云函数通过 cloud.getWXContext() 获取 openid
      → 查询 users 集合，不存在则自动创建
      → 返回用户信息
    → 存入 globalData / storage
```

**无需用户手动授权**，使用微信云开发匿名登录 + openid 作为唯一标识。

---

## 八、性能优化策略

| 优化点 | 方案 |
|--------|------|
| **图片资源** | 使用微信云存储 CDN，支持压缩参数 `?imageView2/2/w/300` |
| **朋友圈分页** | `skip` + `limit` 分页，每次 10 条，配合骨架屏 |
| **聊天记录分页** | 倒序分页加载，进入页面定位到最底部 |
| **数据预加载** | `moments`、`figures`、`dna_questions` 在 `app.onLaunch` 预缓存到 storage |
| **AI 响应优化** | 云函数设置 30 秒超时，DeepSeek 使用流式输出 |
| **字体加载** | Noto Serif SC / Ma Shan Zheng 使用 `wx.loadFontFace` 按需加载 |
| **代码分包** | 奏折、飞鸽传书等非首屏功能使用 `subpackages` 分包 |

---

## 九、与穿越兰台主站的协同

| 协同点 | 实现方式 |
|--------|----------|
| **24史数据** | 云函数 `shiji` 代理调用 `https://shiji.timeslip.work/api/...` |
| **用户体系** | 小程序独立用户体系（openid），未来可绑定主站账号 |
| **内容引流** | 朋友圈/信件末尾添加 "想亲历这段历史？前往穿越兰台" 入口 |
| **品牌统一** | 小程序名称、Logo、配色与主站保持一致 |

---

## 十、开发排期（基于小程序技术栈的调整版）

| 阶段 | 时间 | 任务 | 产出 |
|------|------|------|------|
| **Phase 1: 框架搭建** | 7.24-7.25 (2天) | 项目初始化、微信风格 UI 框架、自定义 TabBar/导航栏、CSS 变量系统、云函数基础搭建 | 可运行空壳框架 |
| **Phase 2: 聊天模块** | 7.26-7.27 (2天) | 聊天列表页、对话页、AI 对话云函数（DeepSeek 接入）、5 个预设人物 | 聊天功能可用 |
| **Phase 3: 发现页-朋友圈** | 7.28-7.30 (3天) | 朋友圈信息流、点赞评论交互、AI 评论回复、筛选功能 | 朋友圈核心体验 |
| **Phase 4: 发现页-子功能** | 7.31-8.01 (2天) | DNA 测试页、飞鸽传书写信/回信/动画 | 两个子功能可用 |
| **Phase 5: 批奏折** | 8.02-8.03 (2天) | 奏折渲染、决策交互、AI 推演、任务线 | 批奏折完整体验 |
| **Phase 6: 兰台+我的** | 8.04 (1天) | 24史接入（代理）、人物图鉴、个人中心、成就系统 | 四 Tab 完整 |
| **Phase 7: 内容创作** | 7.25-8.03 (并行) | 50-80 条朋友圈 + 评论 + 8-12 道奏折 + DNA 题目 | 全部内容数据 |
| **Phase 8: 联调打磨** | 8.05-8.06 (2天) | 全功能联调、UI 细节打磨、性能优化、内容审核接入 | 稳定可演示 |
| **Phase 9: 缓冲提交** | 8.07-8.09 (3天) | 用户测试、Bug 修复、演示视频录制、提交材料 | 完整提交包 |

---

## 十一、技术风险与应对

| 风险 | 影响 | 应对方案 |
|------|------|----------|
| DeepSeek API 不稳定 | AI 对话/推演失败 | ① 配置 GLM-4 作为 fallback ② 预生成内容兜底 |
| 微信云开发免费额度不足 | 云函数调用受限 | ① 内容数据预生成减少实时调用 ② 监控用量，必要时升级 |
| 24史主站 API 跨域/限流 | 兰台数据无法加载 | ① 云函数代理解决跨域 ② 本地缓存关键数据兜底 |
| 朋友圈内容量过大 | 首屏加载慢 | ① 分页加载 ② 图片懒加载 ③ 骨架屏 |
| Lottie 动画兼容性问题 | 飞鸽动画无法播放 | ① 提供静态图 fallback ② 使用兼容性更好的动画库 |

---

## 十二、与参考项目 intelligentmoms 的对比总结

| 维度 | intelligentmoms | 穿越朋友圈 |
|------|-----------------|------------|
| **框架** | 微信小程序原生 | 微信小程序原生（一致） |
| **UI 库** | 全自定义组件 | 全自定义组件（一致） |
| **后端** | 微信云开发 | 微信云开发（一致） |
| **AI 能力** | 微信原生 AI（agent-ui） | 云函数代理 DeepSeek/GLM-4（差异点） |
| **自定义导航栏** | 有 | 有（一致） |
| **自定义 TabBar** | 有 | 有（一致） |
| **内容安全** | msgSecCheck/imgSecCheck | msgSecCheck/imgSecCheck（一致） |
| **认证** | 自动登录 + openid | 自动登录 + openid（一致） |
| **数据存储** | 云数据库 + 云存储 | 云数据库 + 云存储 + 主站 API 代理（扩展） |
| **Markdown** | 自定义 wd-markdown | 复用 wd-markdown 或 mp-html（复用） |

---

*本方案基于产品设计方案（v2.0）和参考项目 `intelligentmoms` 的技术实践制定。*
